# =======================================
# StackKit GitOps Atlantis Configuration
# =======================================
# Enhanced with Slack notifications, Infracost analysis, and security checks

version: 3

# Global Configuration
automerge: false  # Safety first - manual merge for production
delete_source_branch_on_merge: true

# Projects Configuration  
projects:
- name: shared-infra
  dir: .
  workspace: prod
  terraform_version: v1.7.5
  delete_source_branch_on_merge: true
  repo_locking: true
  
  # Auto-planning
  autoplan:
    when_modified: ["**/*.tf", "**/*.tfvars"]
    enabled: true
  
  # Requirements for apply
  apply_requirements: ["approved", "mergeable"]
  
  # Use enhanced workflow with notifications and cost analysis
  workflow: stackkit-enhanced

# Enhanced Workflows
workflows:
  stackkit-enhanced:
    plan:
      steps:
      - env:
          name: TF_IN_AUTOMATION
          value: "true"
      - init:
          extra_args: ["-upgrade"]
      - plan:
          extra_args: ["-lock-timeout=10m", "-input=false"]
      - run: |
          set -e
          
          # =======================================
          # StackKit Enhanced Plan Analysis
          # =======================================
          
          echo "🚀 StackKit Plan Analysis Starting..."
          
          # Extract repository and PR information
          REPO_ORG=$(echo "$BASE_REPO_OWNER" | tr '[:upper:]' '[:lower:]')
          REPO_NAME=$(echo "$BASE_REPO_NAME" | tr '[:upper:]' '[:lower:]')
          PR_NUM=$PULL_NUM
          COMMIT_SHA=$(echo "$HEAD_COMMIT" | cut -c1-8)
          TIMESTAMP=$(date -u +%Y%m%d%H%M%S)
          PR_URL="https://github.com/${BASE_REPO_OWNER}/${BASE_REPO_NAME}/pull/${PR_NUM}"
          
          # Determine plan status
          if [ -f "$PLANFILE" ]; then
            PLAN_STATUS="succeeded"
            PLAN_COLOR="good"
            echo "✅ Terraform plan succeeded"
          else
            PLAN_STATUS="failed"
            PLAN_COLOR="danger"
            echo "❌ Terraform plan failed"
          fi
          
          # Initialize analysis variables
          CHANGE_SUMMARY=""
          RESOURCE_COUNTS=""
          COST_INFO=""
          SECURITY_INFO=""
          
          # =======================================
          # Resource Change Analysis
          # =======================================
          
          if [ -n "$PLANFILE" ] && [ -f "$PLANFILE" ]; then
            echo "📊 Analyzing Terraform plan changes..."
            
            # Convert plan to JSON for analysis
            if terraform show -json "$PLANFILE" > plan_analysis.json 2>/dev/null; then
              echo "✅ Plan converted to JSON successfully"
              
              # Validate JSON structure
              if jq empty plan_analysis.json 2>/dev/null; then
                echo "✅ JSON structure is valid"
                
                # Extract resource change counts
                if jq 'has("resource_changes")' plan_analysis.json | grep -q true; then
                  ADD_COUNT=$(jq '[.resource_changes[]? | select(.change.actions[]? == "create")] | length' plan_analysis.json 2>/dev/null || echo 0)
                  UPDATE_COUNT=$(jq '[.resource_changes[]? | select(.change.actions[]? == "update")] | length' plan_analysis.json 2>/dev/null || echo 0)  
                  DELETE_COUNT=$(jq '[.resource_changes[]? | select(.change.actions[]? == "delete")] | length' plan_analysis.json 2>/dev/null || echo 0)
                  
                  TOTAL_CHANGES=$((ADD_COUNT + UPDATE_COUNT + DELETE_COUNT))
                  
                  if [ $TOTAL_CHANGES -gt 0 ]; then
                    RESOURCE_COUNTS="📈 *Changes*: +${ADD_COUNT} ~${UPDATE_COUNT} -${DELETE_COUNT}"
                    CHANGE_SUMMARY="Total ${TOTAL_CHANGES} resource changes planned"
                    echo "📊 Resource changes: +${ADD_COUNT} ~${UPDATE_COUNT} -${DELETE_COUNT}"
                  else
                    RESOURCE_COUNTS="✅ *No Changes*: Infrastructure matches desired state"
                    CHANGE_SUMMARY="No resource changes required"
                    echo "✅ No infrastructure changes detected"
                  fi
                else
                  echo "⚠️ No resource_changes found in plan"
                  RESOURCE_COUNTS="ℹ️ *Analysis*: Plan structure could not be parsed"
                fi
              else
                echo "❌ Invalid JSON structure in plan analysis"
              fi
            else
              echo "❌ Failed to convert plan to JSON"
              RESOURCE_COUNTS="⚠️ *Analysis*: Plan could not be analyzed"
            fi
          else
            echo "❌ Plan file not found for analysis"
            RESOURCE_COUNTS="❌ *Error*: Plan file not available"
          fi
          
          # =======================================
          # Infracost Analysis
          # =======================================
          
          echo "💰 Starting Infracost analysis..."
          
          if [ -n "$INFRACOST_API_KEY" ] && command -v infracost >/dev/null 2>&1 && [ -f "$PLANFILE" ]; then
            echo "🔧 Configuring Infracost..."
            
            # Configure Infracost API key
            if infracost configure set api_key "$INFRACOST_API_KEY" >/dev/null 2>&1; then
              echo "✅ Infracost API key configured"
              
              # Run cost analysis
              echo "📊 Running cost breakdown analysis..."
              
              # Use directory-based analysis for better compatibility
              if COST_JSON=$(infracost breakdown --path . --format json 2>/dev/null); then
                echo "✅ Cost analysis completed"
                
                # Extract monthly cost
                if echo "$COST_JSON" | jq empty 2>/dev/null; then
                  MONTHLY_COST=$(echo "$COST_JSON" | jq -r '.totalMonthlyCost // "0"' 2>/dev/null || echo "0")
                  
                  if [ "$MONTHLY_COST" != "null" ] && [ "$MONTHLY_COST" != "0" ]; then
                    COST_INFO="💰 *Estimated Monthly Cost*: \$${MONTHLY_COST}"
                    echo "💰 Monthly cost estimate: \$${MONTHLY_COST}"
                  else
                    COST_INFO="💰 *Cost*: No billable resources detected"
                    echo "💰 No billable resources found"
                  fi
                else
                  echo "❌ Invalid cost analysis JSON"
                  COST_INFO="⚠️ *Cost*: Analysis could not be parsed"
                fi
                
                # Generate Infracost comment (optional)
                if [ "$MONTHLY_COST" != "0" ] && [ "$MONTHLY_COST" != "null" ]; then
                  echo "📝 Generating Infracost comment..."
                  infracost comment github \
                    --repo "$REPO_ORG/$REPO_NAME" \
                    --pull-request "$PR_NUM" \
                    --path . \
                    --github-token "$ATLANTIS_GH_TOKEN" \
                    --behavior update 2>/dev/null || echo "⚠️ Could not post Infracost comment"
                fi
              else
                echo "❌ Cost analysis failed"
                COST_INFO="❌ *Cost*: Analysis failed"
              fi
            else
              echo "❌ Failed to configure Infracost API key"
              COST_INFO="❌ *Cost*: Configuration failed"
            fi
          else
            echo "⚠️ Infracost analysis skipped"
            if [ -z "$INFRACOST_API_KEY" ]; then
              echo "  - INFRACOST_API_KEY not set"
            fi
            if ! command -v infracost >/dev/null 2>&1; then
              echo "  - infracost binary not found"
            fi
            COST_INFO="ℹ️ *Cost*: Analysis not configured"
          fi
          
          # =======================================
          # Security Analysis (Basic)
          # =======================================
          
          echo "🛡️ Running security checks..."
          
          # Check for common security issues in plan
          if [ -f "plan_analysis.json" ]; then
            # Check for public access
            PUBLIC_RESOURCES=$(jq '[.resource_changes[]? | select(.change.after.acl? == "public-read" or .change.after.public_access_block?.block_public_acls? == false)] | length' plan_analysis.json 2>/dev/null || echo 0)
            
            # Check for unencrypted resources
            UNENCRYPTED_RESOURCES=$(jq '[.resource_changes[]? | select(.change.after.encryption? == null or .change.after.server_side_encryption? == null)] | length' plan_analysis.json 2>/dev/null || echo 0)
            
            if [ "$PUBLIC_RESOURCES" -gt 0 ] || [ "$UNENCRYPTED_RESOURCES" -gt 0 ]; then
              SECURITY_INFO="⚠️ *Security*: Review required (${PUBLIC_RESOURCES} public, ${UNENCRYPTED_RESOURCES} unencrypted)"
              echo "⚠️ Security review needed: ${PUBLIC_RESOURCES} public resources, ${UNENCRYPTED_RESOURCES} potentially unencrypted"
            else
              SECURITY_INFO="✅ *Security*: No obvious issues detected"
              echo "✅ Basic security checks passed"
            fi
          else
            SECURITY_INFO="ℹ️ *Security*: Analysis not available"
          fi
          
          # =======================================
          # Slack Notification
          # =======================================
          
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            echo "📨 Sending Slack notification..."
            
            # Create comprehensive Slack message
            SLACK_MESSAGE=$(cat << EOF
          {
            "blocks": [
              {
                "type": "header",
                "text": {
                  "type": "plain_text",
                  "text": "🏗️ StackKit Infrastructure Plan"
                }
              },
              {
                "type": "section",
                "fields": [
                  {
                    "type": "mrkdwn",
                    "text": "*Repository*: ${REPO_ORG}/${REPO_NAME}"
                  },
                  {
                    "type": "mrkdwn", 
                    "text": "*PR*: <${PR_URL}|#${PR_NUM}>"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*Status*: ${PLAN_STATUS}"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*Commit*: ${COMMIT_SHA}"
                  }
                ]
              },
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn",
                  "text": "${RESOURCE_COUNTS}"
                }
              },
              {
                "type": "section", 
                "text": {
                  "type": "mrkdwn",
                  "text": "${COST_INFO}"
                }
              },
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn", 
                  "text": "${SECURITY_INFO}"
                }
              },
              {
                "type": "context",
                "elements": [
                  {
                    "type": "mrkdwn",
                    "text": "🤖 StackKit Atlantis • ${TIMESTAMP}"
                  }
                ]
              }
            ]
          }
          EOF
            )
            
            # Send notification
            if curl -X POST -H 'Content-type: application/json' \
                 --data "$SLACK_MESSAGE" \
                 "$SLACK_WEBHOOK_URL" >/dev/null 2>&1; then
              echo "✅ Slack notification sent successfully"
            else
              echo "❌ Failed to send Slack notification"
            fi
          else
            echo "ℹ️ Slack notification skipped (SLACK_WEBHOOK_URL not set)"
          fi
          
          # Cleanup
          rm -f plan_analysis.json 2>/dev/null || true
          
          echo "🚀 StackKit Plan Analysis Complete!"

    apply:
      steps:
      - env:
          name: TF_IN_AUTOMATION
          value: "true"
      - apply:
          extra_args: ["-auto-approve", "-input=false"]
      - run: |
          set -e
          
          echo "🚀 StackKit Apply Complete!"
          
          # Send apply success notification
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            REPO_ORG=$(echo "$BASE_REPO_OWNER" | tr '[:upper:]' '[:lower:]')
            REPO_NAME=$(echo "$BASE_REPO_NAME" | tr '[:upper:]' '[:lower:]')
            PR_NUM=$PULL_NUM
            PR_URL="https://github.com/${BASE_REPO_OWNER}/${BASE_REPO_NAME}/pull/${PR_NUM}"
            TIMESTAMP=$(date -u +%Y%m%d%H%M%S)
            
            APPLY_MESSAGE=$(cat << EOF
          {
            "blocks": [
              {
                "type": "header", 
                "text": {
                  "type": "plain_text",
                  "text": "✅ StackKit Apply Successful"
                }
              },
              {
                "type": "section",
                "fields": [
                  {
                    "type": "mrkdwn",
                    "text": "*Repository*: ${REPO_ORG}/${REPO_NAME}"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*PR*: <${PR_URL}|#${PR_NUM}>"
                  }
                ]
              },
              {
                "type": "context",
                "elements": [
                  {
                    "type": "mrkdwn", 
                    "text": "🤖 StackKit Atlantis • ${TIMESTAMP}"
                  }
                ]
              }
            ]
          }
          EOF
            )
            
            curl -X POST -H 'Content-type: application/json' \
                 --data "$APPLY_MESSAGE" \
                 "$SLACK_WEBHOOK_URL" >/dev/null 2>&1 || echo "Failed to send apply notification"
          fi

# Global Requirements
apply_requirements:
  - approved
  - mergeable

# Allowed branch patterns
allowed_regexp_prefixes:
  - main
  - master
  - feature/
  - hotfix/
  - release/